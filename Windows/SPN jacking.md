Its combination of two privileges:
1. unconstrained delegation 
2. write SPN 
![[Pasted image 20250903015913.png]]
In a case like this we have `Gabriel` is admin to `SRV01` and have `WriteSPN` on `WEB01` so what we can do is